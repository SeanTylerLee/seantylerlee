/**
 * Optional Google Maps JavaScript API key — when set, route lines use Google Directions first,
 * then Mapbox Directions, then OSRM. The basemap stays Mapbox.
 *
 * In Google Cloud Console: enable "Maps JavaScript API", restrict the key by HTTP referrer,
 * and avoid exposing an unrestricted key in public repos.
 */
window.GOOGLE_MAPS_API_KEY = "";
